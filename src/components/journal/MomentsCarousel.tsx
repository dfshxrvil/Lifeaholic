import { Lightbulb, Sparkles } from 'lucide-react-native';
import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { AnimatedPressable } from '@/components/ui/AnimatedPressable';
import { useTheme } from '@/contexts/ThemeContext';

export const moments = [{ category: 'Gratitude', prompt: 'What felt quietly good today?' }, { category: 'Reflection', prompt: 'What would you do differently?' }, { category: 'Growth', prompt: 'What did today teach you?' }, { category: 'People', prompt: 'Who made the day meaningful?' }];
export function MomentsCarousel({ onSelect }: { onSelect: (category: string, prompt: string) => void }) { const { colors } = useTheme(); return <View><View style={styles.heading}><Sparkles size={16} color={colors.journalAccent} /><Text style={[styles.title, { color: colors.text }]}>Moments</Text></View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.list}>{moments.map((item) => <AnimatedPressable key={item.category} onPress={() => onSelect(item.category, item.prompt)} style={[styles.card, { backgroundColor: `${colors.journalAccent}18`, borderColor: `${colors.journalAccent}48` }]}><Lightbulb size={17} color={colors.journalAccent} /><Text style={[styles.category, { color: colors.journalAccent }]}>{item.category}</Text><Text style={[styles.prompt, { color: colors.text }]}>{item.prompt}</Text></AnimatedPressable>)}</ScrollView></View>; }
const styles = StyleSheet.create({ heading: { flexDirection: 'row', alignItems: 'center', gap: 6, marginBottom: 8 }, title: { fontSize: 16, fontWeight: '700' }, list: { gap: 9 }, card: { width: 180, minHeight: 105, borderWidth: StyleSheet.hairlineWidth, borderRadius: 18, padding: 13 }, category: { fontSize: 9, fontWeight: '800', textTransform: 'uppercase', letterSpacing: 0.8, marginTop: 8 }, prompt: { fontSize: 13, fontWeight: '600', lineHeight: 17, marginTop: 4 } });
