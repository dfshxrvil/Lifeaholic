import { PlayCircle } from 'lucide-react-native';
import { Image, StyleSheet, View } from 'react-native';
export function MediaGrid({ urls }: { urls: string[] }) { const visible = urls.slice(0, 4); if (!visible.length) return null; return <View style={styles.grid}>{visible.map((url, index) => { const style = [styles.image, visible.length === 1 && styles.single, visible.length === 3 && index === 0 && styles.hero]; const video = /\.(mp4|mov|webm)(\?|$)/i.test(url); return video ? <View key={`${url}-${index}`} style={[style, styles.video]}><PlayCircle size={36} color="#FFF" /></View> : <Image key={`${url}-${index}`} source={{ uri: url }} resizeMode="cover" style={style} />; })}</View>; }
const styles = StyleSheet.create({ grid: { height: 178, flexDirection: 'row', flexWrap: 'wrap', gap: 3, overflow: 'hidden', borderRadius: 16, marginTop: 10 }, image: { width: '49.5%', height: '49.5%' }, single: { width: '100%', height: '100%' }, hero: { width: '100%', height: '59%' }, video: { backgroundColor: '#343438', alignItems: 'center', justifyContent: 'center' } });
