import { useLocalSearchParams, useRouter } from 'expo-router';
import { BarChart3, ChevronLeft, ChevronRight, Plus, Search, Settings2, Trash2, Users } from 'lucide-react-native';
import { useDeferredValue, useEffect, useState } from 'react';
import { ActivityIndicator, Alert, FlatList, Platform, RefreshControl, StyleSheet, Text, TextInput, View } from 'react-native';
import { Swipeable } from 'react-native-gesture-handler';
import Animated, { FadeInDown, useReducedMotion } from 'react-native-reanimated';
import { AddExpenseModal } from '@/components/finance/AddExpenseModal';
import { AnalyticsModal } from '@/components/finance/AnalyticsModal';
import { GroupModal } from '@/components/finance/GroupModal';
import { useFloatingTabBarMetrics } from '@/components/navigation/FloatingTabBar';
import { AnimatedPressable } from '@/components/ui/AnimatedPressable';
import { Screen } from '@/components/ui/Screen';
import { SlidingSegmentedControl } from '@/components/ui/SlidingSegmentedControl';
import { collapseOut } from '@/constants/animations';
import { motion, typography } from '@/constants/theme';
import { useAuth } from '@/contexts/AuthContext';
import { useTheme } from '@/contexts/ThemeContext';
import { formatPaiseAsInr } from '@/domain/finance';
import { useCombinedGroupBalances, useExpenseMutations, useFinanceExpenses, useFinanceGroups, useGroupBalances } from '@/hooks/finance/useFinance';
import type { FinanceExpense } from '@/types/finance';
import { toDateKey } from '@/utils/dates';

type Mode = 'personal' | 'group';

export function FinanceScreen() {
  const router = useRouter(); const { compose } = useLocalSearchParams<{ compose?: string }>(); const { colors, theme } = useTheme(); const { user } = useAuth();
  const reduceMotion = useReducedMotion(); const { actionBottom, contentBottom } = useFloatingTabBarMetrics();
  const [mode, setMode] = useState<Mode>('personal'); const [search, setSearch] = useState(''); const deferredSearch = useDeferredValue(search);
  const [groupId, setGroupId] = useState<string | null>(null); const [addOpen, setAddOpen] = useState(false); const [groupsOpen, setGroupsOpen] = useState(false); const [analyticsOpen, setAnalyticsOpen] = useState(false); const [editing, setEditing] = useState<FinanceExpense | null>(null);
  const [month, setMonth] = useState(() => toDateKey(new Date()).slice(0, 7));
  const groupsQuery = useFinanceGroups();
  const expenses = useFinanceExpenses({ scope: mode, monthStart: `${month}-01`, groupId: mode === 'group' ? groupId : null, search: deferredSearch, limit: 30 });
  const selectedBalances = useGroupBalances(mode === 'group' ? groupId : null);
  const combinedBalances = useCombinedGroupBalances(mode === 'group' && !groupId ? groupsQuery.groups.map((group) => group.id) : []);
  const expenseMutation = useExpenseMutations(); const balance = groupId ? selectedBalances.balances : combinedBalances.balances;

  useEffect(() => { if (compose === 'expense') { setAnalyticsOpen(false); setGroupsOpen(false); setEditing(null); setAddOpen(true); router.setParams({ compose: undefined }); } }, [compose, router]);
  useEffect(() => { if (groupId && !groupsQuery.groups.some((group) => group.id === groupId)) setGroupId(null); }, [groupId, groupsQuery.groups]);
  const stepMonth = (offset: number) => { const date = new Date(`${month}-01T12:00:00`); date.setMonth(date.getMonth() + offset); setMonth(toDateKey(date).slice(0, 7)); };
  const changeMode = (next: Mode) => { setMode(next); setSearch(''); if (next === 'personal') setGroupId(null); };
  const archive = (expense: FinanceExpense) => {
    const perform = () => void expenseMutation.archiveExpense(expense.id).catch(() => undefined);
    if (Platform.OS === 'web') { if (typeof window !== 'undefined' && window.confirm(`Archive expense?\n\n${expense.description}`)) perform(); return; }
    Alert.alert('Archive expense?', expense.description, [{ text: 'Cancel', style: 'cancel' }, { text: 'Archive', style: 'destructive', onPress: perform }]);
  };
  const renderExpense = ({ item, index }: { item: FinanceExpense; index: number }) => {
    const participant = item.participants.find((entry) => entry.userId === user?.id); const effect = (participant?.amountPaidMinor ?? 0n) - (participant?.amountOwedMinor ?? 0n);
    const displayAmount = item.groupId ? effect : item.totalAmountMinor;
    const payerIsYou = (participant?.amountPaidMinor ?? 0n) > 0n; const groupName = groupsQuery.groups.find((group) => group.id === item.groupId)?.name ?? 'Group';
    const transaction = <AnimatedPressable accessibilityLabel={`Edit ${item.description}`} onPress={() => { setEditing(item); setAddOpen(true); }} style={[styles.transaction, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={[styles.transactionDateBlock, { backgroundColor: colors.accentSoft }]}><Text style={[styles.transactionDate, { color: colors.accent }]}>{new Date(`${item.expenseDate}T12:00:00`).toLocaleDateString(undefined, { day: 'numeric', month: 'short' })}</Text></View><View style={styles.transactionMain}><Text numberOfLines={2} style={[styles.transactionTitle, { color: colors.text }]}>{item.description}</Text><Text numberOfLines={1} style={[styles.transactionMeta, { color: colors.textMuted }]}>{item.category} · {item.groupId ? groupName : 'Personal'} · {payerIsYou ? 'You paid' : 'Shared expense'}</Text></View><View style={styles.amountColumn}><Text style={[styles.transactionAmount, { color: item.groupId && displayAmount > 0n ? colors.success : item.groupId && displayAmount < 0n ? colors.danger : colors.text }]}>{item.groupId && displayAmount > 0n ? '+' : item.groupId && displayAmount < 0n ? '−' : ''}{formatPaiseAsInr(displayAmount < 0n ? -displayAmount : displayAmount)}</Text>{item.groupId && <Text style={[styles.share, { color: colors.textMuted }]}>total {formatPaiseAsInr(item.totalAmountMinor)}</Text>}</View><AnimatedPressable accessibilityLabel={`Archive ${item.description}`} onPress={(event) => { event.stopPropagation(); archive(item); }} hitSlop={8} style={styles.delete}><Trash2 size={15} color={colors.textMuted} /></AnimatedPressable></AnimatedPressable>;
    const animated = <Animated.View entering={reduceMotion ? undefined : FadeInDown.delay(Math.min(index, 10) * motion.stagger).duration(260)} exiting={reduceMotion ? undefined : collapseOut}>{transaction}</Animated.View>;
    if (Platform.OS === 'web') return animated;
    return <Swipeable overshootRight={false} renderRightActions={() => <AnimatedPressable accessibilityLabel={`Archive ${item.description}`} onPress={() => archive(item)} style={[styles.swipeDelete, { backgroundColor: colors.danger }]}><Trash2 size={20} color="#FFF" /><Text style={styles.swipeText}>Archive</Text></AnimatedPressable>}>{animated}</Swipeable>;
  };
  const net = balance?.netAmountMinor ?? 0n;

  return <Screen contentStyle={[styles.screen, { paddingBottom: contentBottom }]}><View style={styles.header}><View><Text style={[styles.title, { color: colors.text }]}>Money, made clear.</Text><Text style={[styles.subtitle, { color: colors.textMuted }]}>Your month at a glance</Text></View><AnimatedPressable accessibilityLabel="Manage groups" onPress={() => setGroupsOpen(true)} style={[styles.manage, { backgroundColor: colors.card, borderColor: colors.border }]}><Users size={20} color={colors.accent} /><Settings2 size={13} color={colors.textMuted} /></AnimatedPressable></View>
    <SlidingSegmentedControl value={mode} onChange={changeMode} options={[{ value: 'personal', label: 'Personal Expenses' }, { value: 'group', label: 'Group Expenses' }]} />
    <View style={styles.monthRow}><AnimatedPressable accessibilityLabel="Previous month" onPress={() => stepMonth(-1)} style={styles.monthButton}><ChevronLeft size={20} color={colors.text} /></AnimatedPressable><Text style={{ color: colors.text, fontWeight: '700' }}>{new Date(`${month}-01T12:00:00`).toLocaleDateString(undefined, { month: 'long', year: 'numeric' })}</Text><AnimatedPressable accessibilityLabel="Next month" onPress={() => stepMonth(1)} style={styles.monthButton}><ChevronRight size={20} color={colors.text} /></AnimatedPressable></View>
    <View style={[styles.summary, theme === 'light' && styles.summaryLight, { backgroundColor: theme === 'light' && mode === 'personal' ? '#E8F0FE' : mode === 'personal' ? colors.accentSoft : colors.cardElevated, borderColor: theme === 'light' ? colors.border : colors.accent }]}><View><Text style={[styles.summaryLabel, { color: colors.textMuted }]}>{mode === 'personal' ? 'MONTHLY TOTAL' : 'YOUR NET BALANCE'}</Text><Text style={[styles.summaryAmount, { color: mode === 'group' && net < 0n ? colors.danger : colors.text }]}>{mode === 'group' && net < 0n ? '−' : ''}{formatPaiseAsInr(mode === 'personal' ? expenses.totalAmountMinor : net < 0n ? -net : net)}</Text></View>{mode === 'personal' ? <Text style={[styles.summaryNote, { color: colors.textMuted }]}>{new Date(`${month}-01T12:00:00`).toLocaleDateString(undefined, { month: 'long' })}</Text> : <View style={styles.balanceBreakdown}><Text style={[styles.owed, { color: colors.success }]}>Owed {formatPaiseAsInr(balance?.owedToYouMinor ?? 0n)}</Text><Text style={[styles.owe, { color: colors.danger }]}>Owe {formatPaiseAsInr(balance?.youOweMinor ?? 0n)}</Text></View>}</View>
    {mode === 'group' && <FlatList horizontal data={[{ id: '', name: 'All groups' }, ...groupsQuery.groups]} keyExtractor={(item) => item.id || 'all'} showsHorizontalScrollIndicator={false} contentContainerStyle={styles.filters} renderItem={({ item }) => <AnimatedPressable onPress={() => setGroupId(item.id || null)} style={[styles.filterChip, { backgroundColor: (groupId ?? '') === item.id ? colors.accentSoft : colors.card, borderColor: (groupId ?? '') === item.id ? colors.accent : colors.border }]}><Text style={[styles.filterText, { color: colors.text }]}>{item.name}</Text></AnimatedPressable>} />}
    <View style={[styles.search, { backgroundColor: colors.card, borderColor: colors.border }]}><Search size={17} color={colors.textMuted} /><TextInput accessibilityLabel="Search expenses" value={search} onChangeText={setSearch} placeholder="Search this month" placeholderTextColor={colors.textMuted} style={[styles.searchInput, { color: colors.text }]} /></View>
    <View style={styles.listHeader}><Text style={[styles.listTitle, { color: colors.text }]}>Recent transactions</Text><Text style={[styles.listCount, { color: colors.textMuted }]}>{expenses.expenses.length} entries</Text></View>
    {expenses.loading && !expenses.expenses.length ? <ActivityIndicator color={colors.accent} style={styles.loader} /> : <FlatList data={expenses.expenses} keyExtractor={(item) => item.id} renderItem={renderExpense} contentContainerStyle={[styles.list, !expenses.expenses.length && styles.emptyList]} onEndReached={() => void expenses.loadMore()} onEndReachedThreshold={0.4} refreshControl={<RefreshControl refreshing={expenses.loading} onRefresh={() => void expenses.refresh()} tintColor={colors.accent} />} ListFooterComponent={expenses.loadingMore ? <ActivityIndicator color={colors.accent} /> : null} ListEmptyComponent={<View style={styles.empty}><Text style={[styles.emptyTitle, { color: colors.text }]}>{search.trim() ? 'No matching expenses.' : 'Nothing logged here yet.'}</Text><Text style={[styles.emptySubtitle, { color: colors.textMuted }]}>{search.trim() ? 'Try a different search.' : 'Tap + to add the first expense.'}</Text></View>} />}
    {(expenses.error || expenseMutation.error || groupsQuery.error) && <Text style={[styles.error, { color: colors.danger }]}>{expenses.error || expenseMutation.error || groupsQuery.error}</Text>}
    <AnimatedPressable accessibilityLabel="Open financial analytics" onPress={() => setAnalyticsOpen(true)} style={[styles.analyticsFab, { bottom: actionBottom, backgroundColor: colors.cardElevated, borderColor: colors.accent }]}><BarChart3 size={19} color={colors.accent} /><Text style={[styles.analyticsFabText, { color: colors.text }]}>Analytics</Text></AnimatedPressable>
    <AnimatedPressable accessibilityLabel="Add expense" onPress={() => { setEditing(null); setAddOpen(true); }} style={[styles.fab, { bottom: actionBottom, backgroundColor: colors.accent }]}><Plus size={27} color={colors.buttonText} strokeWidth={2.5} /></AnimatedPressable>
    {addOpen && <AddExpenseModal visible groups={groupsQuery.groups} expense={editing} onClose={() => { setAddOpen(false); setEditing(null); }} onSaved={(saved) => { setMonth(saved.expenseDate.slice(0, 7)); setMode(saved.groupId ? 'group' : 'personal'); setGroupId(saved.groupId); setAddOpen(false); setEditing(null); }} />}
    {groupsOpen && <GroupModal visible onClose={() => setGroupsOpen(false)} />}
    {analyticsOpen && <AnalyticsModal visible onClose={() => setAnalyticsOpen(false)} month={month} scope={mode} groupId={mode === 'group' ? groupId : null} groupName={groupId ? groupsQuery.groups.find((group) => group.id === groupId)?.name : null} />}
  </Screen>;
}

const styles = StyleSheet.create({
  screen: { paddingTop: 16, paddingBottom: 96, gap: 12 }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, title: { fontFamily: typography.display, fontSize: 29, fontWeight: '800' }, subtitle: { fontSize: 12, marginTop: 1 }, manage: { width: 50, height: 44, borderWidth: StyleSheet.hairlineWidth, borderRadius: 14, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 1 }, monthRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, monthButton: { padding: 10 }, summary: { minHeight: 102, borderWidth: StyleSheet.hairlineWidth, borderRadius: 19, padding: 15, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', shadowColor: '#000', shadowOpacity: 0.12, shadowRadius: 14, shadowOffset: { width: 0, height: 6 }, elevation: 4 }, summaryLight: { shadowOpacity: 0.06, shadowRadius: 10, shadowOffset: { width: 0, height: 4 }, elevation: 0 }, summaryLabel: { fontSize: 10, fontWeight: '800', letterSpacing: 1 }, summaryAmount: { fontSize: 34, fontWeight: '700', marginTop: 3 }, summaryNote: { fontSize: 12, fontWeight: '700' }, balanceBreakdown: { alignItems: 'flex-end', gap: 4 }, owed: { fontSize: 11, fontWeight: '700' }, owe: { fontSize: 11, fontWeight: '700' }, filters: { gap: 7 }, filterChip: { borderWidth: StyleSheet.hairlineWidth, borderRadius: 12, paddingHorizontal: 12, paddingVertical: 7 }, filterText: { fontSize: 10, fontWeight: '700' }, search: { minHeight: 43, borderWidth: StyleSheet.hairlineWidth, borderRadius: 13, paddingHorizontal: 12, flexDirection: 'row', alignItems: 'center', gap: 8 }, searchInput: { flex: 1, fontSize: 13 }, listHeader: { flexDirection: 'row', alignItems: 'baseline', justifyContent: 'space-between' }, listTitle: { fontSize: 15, fontWeight: '700' }, listCount: { fontSize: 10, fontWeight: '700' }, list: { gap: 7, paddingBottom: 80 }, emptyList: { flexGrow: 1 }, transaction: { minHeight: 74, borderWidth: StyleSheet.hairlineWidth, borderRadius: 15, padding: 9, flexDirection: 'row', alignItems: 'center', gap: 9 }, transactionDateBlock: { width: 48, height: 38, borderRadius: 12, alignItems: 'center', justifyContent: 'center' }, transactionMain: { flex: 1, minWidth: 0 }, transactionDate: { fontSize: 10, fontWeight: '800' }, transactionTitle: { fontSize: 13, fontWeight: '700' }, transactionMeta: { fontSize: 9, marginTop: 3 }, amountColumn: { alignItems: 'flex-end' }, transactionAmount: { fontSize: 14, fontWeight: '800' }, share: { fontSize: 8, marginTop: 3 }, delete: { padding: 3 }, swipeDelete: { width: 82, marginLeft: 6, borderRadius: 15, alignItems: 'center', justifyContent: 'center', gap: 3 }, swipeText: { color: '#FFF', fontSize: 10, fontWeight: '700' }, empty: { flex: 1, alignItems: 'center', justifyContent: 'center' }, emptyTitle: { fontFamily: typography.display, fontSize: 22, fontWeight: '700' }, emptySubtitle: { fontSize: 11 }, loader: { marginTop: 40 }, error: { position: 'absolute', bottom: 87, left: 20, fontSize: 11, fontWeight: '700' }, fab: { position: 'absolute', right: 22, bottom: 100, width: 54, height: 54, borderRadius: 27, alignItems: 'center', justifyContent: 'center', shadowColor: '#000', shadowOpacity: 0.2, shadowRadius: 12, shadowOffset: { width: 0, height: 6 }, elevation: 6 },
  analyticsFab: { position: 'absolute', left: 22, minHeight: 46, borderRadius: 23, borderWidth: StyleSheet.hairlineWidth, paddingHorizontal: 15, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 7, shadowColor: '#000', shadowOpacity: 0.14, shadowRadius: 10, shadowOffset: { width: 0, height: 5 }, elevation: 5 },
  analyticsFabText: { fontSize: 11, fontWeight: '800' },
});
